1. subset traversing in DP: dfs(i+1, currentTarget - nums[i]) || dfs(i+1, currentTarget) // subtract the current element or not.
2. Longest Increasing Subset: if(nums[j] > nums[i]); For loop in DFS to visit every subset.
3. word break: if(wordDict.includes(s.slice(start,end)) && dfs(end))