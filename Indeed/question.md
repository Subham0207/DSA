User search for jobs, Lets say user searches using only two keywords.
Each job has description
For the jobs that match the keyword, You task is show the substring that contains the keywords.

Note: no need to implment job search, The jobs have already been selected, Now you need to show a substring for each selected job as description in search result.


This is the code you need to finish.

Target: Time comlexity O(m+n)

Hint: Use constructor to cache each word to indexes they appear in a datastructure.

Class Job
{
    constructor()
    {

    }

    result(word1, word2)
    {

    }
}